---
title: FTP
date: 2026-04-29
categories:
  - security
comments: true
tags:
  - 윈도우
  - DHCP
  - FTP
  - GNS
---
---
gns3, vmware를 같이쓰면 cpu사용량이 99%까지 올라갈 수 있음
-> virtualbox사용(network card만 7가지)

gns3는 라우터만 제공한다. 스위치로 cli접속해서 설정할 수 없다. 이 때 필요한게 iou다.
iou는 리눅스 가상머신으로만 사용가능하다.

rocky9-1: dhcp
rocky9-2: vsftpd

이게 활성화되어야 vm에서 nat작동
![](../../../assets/images/Pasted%20image%2020260429094548.png)

windows에서는 netstat -na(n은 포트를 서비스로 바꾸지마라, a는 tcp/udp 다 보여줘)

tcp만보려면?
-> netstat -na -p tcp
![](../../../assets/images/Pasted%20image%2020260429094831.png)
만약에 내가 60022로 포트포워딩을 했다. -> nat작동x -> 가상머신 인터넷 멈춤


추가된거 익명사용자
12
28
32

밑에 deny_file도 추가가능
deny_file={\*.txt,\*.exe}


ESTAB연결되어있다
LISTEN 대기중이다.

opffermessage: 지연시간이 느리면 받는 속도도 느려짐
dhcp는 4단계: discover - offer - request - act

주소임대 = var /etc/vsftpd/vsftpd.conf 내용과 같음

서버옵션은 범위를 여러개 만들 수 있지만 범위 옵션을 설정하면 오버롤 시켜서 범위가 적용된다.

서버옵션: 전역변수 느낌
범위옵션: 지역변수 느낌

영향범위가 작은경우가 힘이 더 쎔(동일한 경우)
![](../../../assets/images/Pasted%20image%2020260429104258.png)
10.0.0.254 -> 10.0.0.252 (종이모양 -> 컴퓨터모양 바뀜)

윈도우는 하위단에 없으면 동일한 옵션경우는 상속됨
(공유폴더 만드는법)


---
iis체크
(역할서비스를 직접 체크해야됨, 관리콘솔은 필수)

compmgmt.msc


ftp 추가
![](../../../assets/images/Pasted%20image%2020260429111848.png)
![](../../../assets/images/Pasted%20image%2020260429112242.png)
만약에 삭제를하고 나서 해당 항목을 허용으로 바꿔줘야 한다.
화이트리스트, 블랙리스트 차이 알기

ftp 권한 부여 규칙과 ftp인증은 하나이다. 같이 작업
디렉토리 검색: ms-dos는 리눅스용이기때문에 윈도우의 권한을 모름 -> unix로 설정 -> 적용

iis를 설치하면 inetpub이 생김(해커들도알고있음 -> 기본폴더 사용x)
로그파일은 현지 시간사용안하고 세계 표준 시간으로 설정함

ftp메시지
배너설정가능
변수사용할땐 %username%으로 하면됨 대신 메시지에서 사용자변수지원을 체크해야 가능

ftp 방화벽 지원
전역설정에서 패시브모드 설정을 할 수 있음(데이터 채널 포트 범위)

ftp 사용자 격리
chroot부분임

윈도우는 역할을 설치하면 해당 기능에 대한 포트를 열어준다(기본만)
ftp만 문제가 좀 있어서 수동으로 인바운드규칙(20,21)포트추가해줘야 함
shift+폴더 하면 파워쉘열기생김

(linux에서의 truncate, dd)
윈도우는? -> fsutil file createnew ftp.txt 1024
윈도우는 ftp> 상태에서 다른 사용자로 변경가능

로컬확인 !dir
서버확인 dir

로컬이동 lcd
서버이동 cd

목록: dir --------> LIST
다운로드: get ----> RETR
업로드: put -------> STOR


사용자 이름 실제 디렉토리(전역 가상 디레토리 사용) 
--> LocalUser폴더를 무조건적으로 생성해줘야 함

---

# 보안 수업 노트 — FTP (2026-04-29)

---

## 1. 환경 구성

### GNS3 + VMware 주의사항
- GNS3와 VMware를 함께 사용하면 CPU 사용량이 **99%** 까지 올라갈 수 있음
- 대안: **VirtualBox** 사용 (네트워크 카드 종류만 7가지 제공)

### GNS3의 한계와 IOU
- GNS3는 **라우터**만 제공 → 스위치에 CLI로 접속해 설정 불가
- 이를 해결하기 위해 **IOU(IOS on Unix)** 사용
  - IOU는 **리눅스 가상머신**에서만 사용 가능

### 실습 VM 구성
| VM | 역할 |
|---|---|
| rocky9-1 | DHCP 서버 |
| rocky9-2 | vsftpd (FTP 서버) |

---

## 2. 네트워크 기초

### NAT & 포트포워딩
- VM에서 NAT가 동작하려면 해당 옵션이 **활성화**되어 있어야 함
- 포트포워딩(예: 60022)을 걸면 → NAT 미동작 → 가상머신 인터넷 끊김

### netstat 명령어 (Windows)
```
netstat -na          # n: 포트를 서비스명으로 변환하지 않음 / a: TCP+UDP 모두 표시
netstat -na -p tcp   # TCP만 확인
```

| 상태 | 의미 |
|---|---|
| ESTAB | 연결 중 |
| LISTEN | 대기 중 |

---

## 3. DHCP

### 동작 4단계
```
Discover → Offer → Request → ACK
```
- **Offer Message**: 지연시간이 느리면 받는 속도도 느려짐
- DHCP 임대 주소 파일의 내용 형식은 `vsftpd.conf` 형식과 유사함

### 서버 옵션 vs 범위 옵션
| 구분 | 개념 비유 | 우선순위 |
|---|---|---|
| 서버 옵션 | 전역 변수 | 낮음 |
| 범위 옵션 | 지역 변수 | 높음 (오버라이드) |

> 영향 범위가 **좁을수록** 우선순위가 더 높다.  
> Windows는 하위 단에 옵션이 없으면 동일한 옵션을 **상속**한다. (→ 공유폴더 만드는 법과 동일한 원리)

### GNS3 토폴로지 아이콘 변화
- `10.0.0.254` → `10.0.0.252` 로 변경되면서 아이콘이 **종이 모양 → 컴퓨터 모양**으로 바뀜

---

## 4. vsftpd (Linux FTP 서버)

### 주요 설정 항목 (vsftpd.conf)
- 익명 사용자 추가: 라인 12, 28, 32 참고
- 파일 거부 설정 예시:
  ```
  deny_file={\*.txt,\*.exe}
  ```

---

## 5. IIS FTP (Windows FTP 서버)

### IIS 설치
- **역할 서비스를 직접 체크**해야 함 (관리 콘솔은 필수 선택)
- 실행: `compmgmt.msc`
- IIS 설치 시 `inetpub` 폴더 자동 생성
  - ⚠️ 해커도 기본 경로를 알고 있으므로 **기본 폴더 사용 금지**

### 주요 설정 항목
| 항목 | 설명 |
|---|---|
| FTP 권한 부여 규칙 + FTP 인증 | 항상 함께 작업 |
| 디렉토리 검색 | MS-DOS는 리눅스용 → 윈도우 권한 인식 불가 → **UNIX**로 설정 |
| 로그 파일 시간 | 현지 시간 사용 안 함 → **세계 표준시(UTC)** 사용 |
| FTP 메시지(배너) | 변수 사용 시 `%username%` 형식, "메시지에서 사용자 변수 지원" 체크 필요 |
| FTP 방화벽 지원 | 전역 설정에서 패시브 모드 / 데이터 채널 포트 범위 설정 가능 |
| FTP 사용자 격리 | Linux의 `chroot`와 동일한 개념 |

### 화이트리스트 vs 블랙리스트
- 항목 **삭제** 후 해당 항목을 **허용**으로 변경해야 정상 동작

### 방화벽 포트 수동 추가
- Windows는 역할 설치 시 기본 포트만 자동 개방
- FTP는 예외적으로 **인바운드 규칙에 20, 21번 포트를 수동 추가** 필요

### 유용한 팁
- `Shift + 폴더 우클릭` → PowerShell 열기 메뉴 생성

---

## 6. FTP 클라이언트 명령어

### 파일 생성 (빈 파일)
| OS | 명령어 |
|---|---|
| Linux | `truncate`, `dd` |
| Windows | `fsutil file createnew ftp.txt 1024` |

### FTP 명령어 대응표
| 클라이언트 명령 | FTP 프로토콜 명령 | 설명 |
|---|---|---|
| `dir` | `LIST` | 서버 목록 |
| `get` | `RETR` | 다운로드 |
| `put` | `STOR` | 업로드 |
| `!dir` | — | 로컬 목록 확인 |
| `lcd` | — | 로컬 디렉토리 이동 |
| `cd` | — | 서버 디렉토리 이동 |

> Windows FTP 클라이언트에서는 `ftp>` 상태에서 **다른 사용자로 전환** 가능

---

## 7. FTP 사용자 격리 (가상 디렉토리)

- **전역 가상 디렉토리 사용** 시 `LocalUser` 폴더를 **반드시 생성**해야 함
- 사용자 이름 기준으로 실제 디렉토리가 매핑됨

---

# 보안 수업 노트 — FTP (2026-04-29)

d
---

## 1. 환경 구성

### GNS3 + VMware 주의사항
- GNS3와 VMware를 함께 사용하면 CPU 사용량이 **99%** 까지 올라갈 수 있음
- 대안: **VirtualBox** 사용 (네트워크 카드 종류만 7가지 제공)

### GNS3의 한계와 IOU
- GNS3는 **라우터**만 제공 → 스위치에 CLI로 접속해 설정 불가
- 이를 해결하기 위해 **IOU(IOS on Unix)** 사용
  - IOU는 **리눅스 가상머신**에서만 사용 가능

### 실습 VM 구성
| VM | 역할 |
|---|---|
| rocky9-1 | DHCP 서버 |
| rocky9-2 | vsftpd (FTP 서버) |

---

## 2. 네트워크 기초

### NAT & 포트포워딩
- VM에서 NAT가 동작하려면 해당 옵션이 **활성화**되어 있어야 함

>  VMware NAT Service 활성화 확인
> ![](../../../assets/images/Pasted%20image%2020260429140211.png)

- 포트포워딩(예: 60022)을 걸면 → NAT 미동작 → 가상머신 인터넷 끊김

### netstat 명령어 (Windows)
```
netstat -na          # n: 포트를 서비스명으로 변환하지 않음 / a: TCP+UDP 모두 표시
netstat -na -p tcp   # TCP만 확인
```

| 상태 | 의미 |
|---|---|
| ESTAB | 연결 중 |
| LISTEN | 대기 중 |

> netstat -na 실행 결과 (LISTENING / ESTABLISHED 상태 확인)
> ![](../../../assets/images/Pasted%20image%2020260429140231.png)

---

## 3. DHCP

### 동작 4단계
```
Discover → Offer → Request → ACK
```
- **Offer Message**: 지연시간이 느리면 받는 속도도 느려짐
- DHCP 임대 주소 파일의 내용 형식은 `vsftpd.conf` 형식과 유사함

### 서버 옵션 vs 범위 옵션
| 구분 | 개념 비유 | 우선순위 |
|---|---|---|
| 서버 옵션 | 전역 변수 | 낮음 |
| 범위 옵션 | 지역 변수 | 높음 (오버라이드) |

> 영향 범위가 **좁을수록** 우선순위가 더 높다.  
> Windows는 하위 단에 옵션이 없으면 동일한 옵션을 **상속**한다. (→ 공유폴더 만드는 법과 동일한 원리)

> DHCP 관리 콘솔 — 범위 옵션(003 라우터, 006 DNS 서버, 015 DNS 도메인) 및 예약 설정 화면
> ![](../../../assets/images/Pasted%20image%2020260429140307.png)

### GNS3 토폴로지 아이콘 변화
- `10.0.0.254` → `10.0.0.252` 로 변경되면서 아이콘이 **종이 모양 → 컴퓨터 모양**으로 바뀜

---

## 4. vsftpd (Linux FTP 서버)

### 주요 설정 항목 (vsftpd.conf)
- 익명 사용자 추가: 라인 12, 28, 32 참고
- 파일 거부 설정 예시:
  ```
  deny_file={\*.txt,\*.exe}
  ```

---

## 5. IIS FTP (Windows FTP 서버)

### IIS 설치
- **역할 서비스를 직접 체크**해야 함 (관리 콘솔은 필수 선택)
- 실행: `compmgmt.msc`
- IIS 설치 시 `inetpub` 폴더 자동 생성
  - ⚠️ 해커도 기본 경로를 알고 있으므로 **기본 폴더 사용 금지**

> IIS FTP 사이트 추가 화면 — 사이트 이름: `jhjang_main`, 경로: `C:\01_ftp` (기본 폴더 대신 별도 경로 사용 예시)
> ![](../../../assets/images/Pasted%20image%2020260429141731.png)

### 주요 설정 항목
| 항목 | 설명 |
|---|---|
| FTP 권한 부여 규칙 + FTP 인증 | 항상 함께 작업 |
| 디렉토리 검색 | MS-DOS는 리눅스용 → 윈도우 권한 인식 불가 → **UNIX**로 설정 |
| 로그 파일 시간 | 현지 시간 사용 안 함 → **세계 표준시(UTC)** 사용 |
| FTP 메시지(배너) | 변수 사용 시 `%username%` 형식, "메시지에서 사용자 변수 지원" 체크 필요 |
| FTP 방화벽 지원 | 전역 설정에서 패시브 모드 / 데이터 채널 포트 범위 설정 가능 |
| FTP 사용자 격리 | Linux의 `chroot`와 동일한 개념 → **전역 가상 디렉토리 사용** 시 `LocalUser` 폴더 반드시 생성, 사용자 이름 기준으로 실제 디렉토리 매핑 |

### 화이트리스트 vs 블랙리스트
- 항목 **삭제** 후 해당 항목을 **허용**으로 변경해야 정상 동작

> FTP IP 주소 및 도메인 제한 화면 — `10.0.0.101` 허용, 나머지 클라이언트는 **거부**로 설정 (화이트리스트 방식)
> ![](../../../assets/images/Pasted%20image%2020260429141844.png)

### 방화벽 포트 수동 추가
- Windows는 역할 설치 시 기본 포트만 자동 개방
- FTP는 예외적으로 **인바운드 규칙에 20, 21번 포트를 수동 추가** 필요

### 유용한 팁
- `Shift + 폴더 우클릭` → PowerShell 열기 메뉴 생성

---

## 6. FTP 클라이언트 명령어

### 파일 생성 (빈 파일)
| OS | 명령어 |
|---|---|
| Linux | `truncate`, `dd` |
| Windows | `fsutil file createnew ftp.txt 1024` |

### FTP 명령어 대응표
| 클라이언트 명령 | FTP 프로토콜 명령 | 설명 |
|---|---|---|
| `dir` | `LIST` | 서버 목록 |
| `get` | `RETR` | 다운로드 |
| `put` | `STOR` | 업로드 |
| `!dir` | — | 로컬 목록 확인 |
| `lcd` | — | 로컬 디렉토리 이동 |
| `cd` | — | 서버 디렉토리 이동 |

> Windows FTP 클라이언트에서는 `ftp>` 상태에서 **다른 사용자로 전환** 가능


---

## 다시 재설치

![](../../../assets/images/Pasted%20image%2020260429143140.png)
초기 사용자 생성

default 웹 사이트 -> 삭제 (취약함)

![](../../../assets/images/Pasted%20image%2020260429150917.png)
로컬유저 이름은 정해져있음 (LocalUser)

![](../../../assets/images/Pasted%20image%2020260429151059.png)
경로에 접속할땐 public으로 만들어야 함

경로가 무조건 존재해야 함

![](../../../assets/images/Pasted%20image%2020260429151241.png)
이렇게 만들었을 경우에는 사용자 이름 실제 디렉터리로 설정

ftp루트디렉토리: ftp폴더밑
사용자이름 디렉터리: ftp폴더 밑 - 사용자 이름 폴더 생성 (anonymous-default폴더)
사용자이름 실제 디렉터리: ftp폴더 밑 LocalUser폴더 밑에 사용자이름 폴더 만들고 (anonymous-public)으로 생성


패시브모드 생성
![](../../../assets/images/Pasted%20image%2020260429152001.png)

![](../../../assets/images/Pasted%20image%2020260429151946.png)

![](../../../assets/images/Pasted%20image%2020260429152117.png)
65000-65010 추가

iis에서 재시작 해도 의미없음
	1. 서버 재부팅
	2. ![](../../../assets/images/Pasted%20image%2020260429152245.png)

![](../../../assets/images/Pasted%20image%2020260429152802.png)



---


gns3설치
![](../../../assets/images/Pasted%20image%2020260429154104.png)
![](../../../assets/images/Pasted%20image%2020260429154121.png)

mysql, vim 다 설치x, mysql은 가상머신에서 하는게좋음, 

gns3설치
winpcap.org -> 다운(가벼움)
wireshark설치
npcap설치하지말기


가상머신에 iou 설치해야함
![](../../../assets/images/Pasted%20image%2020260429161100.png)

![](../../../assets/images/Pasted%20image%2020260429161402.png)

이거 못찾으면 cpu사용량 100%까지올라감 ---> 찾을 때까지 기다리기

![](../../../assets/images/Pasted%20image%2020260429162101.png)
![](../../../assets/images/Pasted%20image%2020260429162132.png)
![](../../../assets/images/Pasted%20image%2020260429162140.png)

![](../../../assets/images/Pasted%20image%2020260429162413.png)
![](../../../assets/images/Pasted%20image%2020260429162450.png)
미리 전체크기 할당은 정적할당이므로 선택x

![](../../../assets/images/Pasted%20image%2020260429162637.png)


gns3에서는 cli안됨(스위치)
vm에 iou올려서 l2,l3스위치구현한게있음 --> cli가능

![](../../../assets/images/Pasted%20image%2020260429163448.png)


스위치는 기본으로 켜져있음 / 라우터는 start 필요

![](../../../assets/images/Pasted%20image%2020260429164917.png)




![](../../../assets/images/Pasted%20image%2020260429164259.png)






---

## 자격증 공부

https://learn.microsoft.com/ko-kr/search/?terms=az-700