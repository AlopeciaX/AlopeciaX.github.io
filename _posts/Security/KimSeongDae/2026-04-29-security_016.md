---
title: ftp
date: 2026-04-29
categories:
comments: true
---
---
gns3, vmware를 같이쓰면 cpu사용량이 99%까지 올라갈 수 있음
-> virtualbox사용(network card만 7가지)

gns3는 라우터만 제공한다. 스위치로 cli접속해서 설정할 수 없다. 이 때 필요한게 iou다.
iou는 리눅스 가상머신으로만 사용가능하다.

rocky9-1: dhcp
rocky9-2: vsftpd

이게 활성화되어야 vm에서 nat작동
![](../../../assets/images/Pasted%20image%2020260429094548.png)

windows에서는 netstat -na(n은 포트를 서비스로 바꾸지마라, a는 tcp/udp 다 보여줘)

tcp만보려면?
-> netstat -na -p tcp
![](../../../assets/images/Pasted%20image%2020260429094831.png)
만약에 내가 60022로 포트포워딩을 했다. -> nat작동x -> 가상머신 인터넷 멈춤


추가된거 익명사용자
12
28
32

밑에 deny_file도 추가가능
deny_file={\*.txt,\*.exe}


ESTAB연결되어있다
LISTEN 대기중이다.

opffermessage: 지연시간이 느리면 받는 속도도 느려짐
dhcp는 4단계: discover - offer - request - act

주소임대 = var /etc/vsftpd/vsftpd.conf 내용과 같음

서버옵션은 범위를 여러개 만들 수 있지만 범위 옵션을 설정하면 오버롤 시켜서 범위가 적용된다.

서버옵션: 전역변수 느낌
범위옵션: 지역변수 느낌

영향범위가 작은경우가 힘이 더 쎔(동일한 경우)
![](../../../assets/images/Pasted%20image%2020260429104258.png)
10.0.0.254 -> 10.0.0.252 (종이모양 -> 컴퓨터모양 바뀜)

윈도우는 하위단에 없으면 동일한 옵션경우는 상속됨
(공유폴더 만드는법)


---
iis체크
(역할서비스를 직접 체크해야됨, 관리콘솔은 필수)

compmgmt.msc


ftp 추가
![](../../../assets/images/Pasted%20image%2020260429111848.png)
![](../../../assets/images/Pasted%20image%2020260429112242.png)
만약에 삭제를하고 나서 해당 항목을 허용으로 바꿔줘야 한다.
화이트리스트, 블랙리스트 차이 알기

ftp 권한 부여 규칙과 ftp인증은 하나이다. 같이 작업
디렉토리 검색: ms-dos는 리눅스용이기때문에 윈도우의 권한을 모름 -> unix로 설정 -> 적용

iis를 설치하면 inetpub이 생김(해커들도알고있음 -> 기본폴더 사용x)
로그파일은 현지 시간사용안하고 세계 표준 시간으로 설정함

ftp메시지
배너설정가능
변수사용할땐 %username%으로 하면됨 대신 메시지에서 사용자변수지원을 체크해야 가능

ftp 방화벽 지원
전역설정에서 패시브모드 설정을 할 수 있음(데이터 채널 포트 범위)

ftp 사용자 격리
chroot부분임

윈도우는 역할을 설치하면 해당 기능에 대한 포트를 열어준다(기본만)
ftp만 문제가 좀 있어서 수동으로 인바운드규칙(20,21)포트추가해줘야 함
shift+폴더 하면 파워쉘열기생김

(linux에서의 truncate, dd)
윈도우는? -> fsutil file createnew ftp.txt 1024
윈도우는 ftp> 상태에서 다른 사용자로 변경가능

로컬확인 !dir
서버확인 dir

로컬이동 lcd
서버이동 cd

목록: dir --------> LIST
다운로드: get ----> RETR
업로드: put -------> STOR

