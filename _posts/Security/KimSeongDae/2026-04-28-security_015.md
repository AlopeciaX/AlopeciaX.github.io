---
title: ftp
date: 2026-04-28
categories:
comments: true
---
---

1.FTP(File Transfer Protocol)
	1.1. 대용량 파일 전송 서비스
	1.2. Protocol: TCP
	1.3. Port: 21(제어 및 인증)
	1.4. Data 전송 Port:
		1.4.1. Active(능동): 클라이언트가 포트를 결정, 서버쪽은 20
			1.4.1.1. 클라이언트가 데이터 전송 포트를 결정해서 서버에 통보
			클라이언트가 알려준 포트로 서버가 데이터를 전송
			화이트보드 현상 발생: Client측 포트를 알지 못해서 방화벽에서 차단
		1.4.2. Passive(수동): 데이터 전송 포트 서버가 결정(관리자가 구성)
			1.4.2.1. 클라이언트가 서버쪽에 데이터 전송을 요청하면 서버는 데이터 전송포트를 Client에게 통보
			클라이언트가 서버의 해당 포트에 접속해서 데이터를 전송해 감.
	1.5. 구성
		1.5.1. # dnf install -y vsftpd
	1.6 설정 파일 위치
		1.6.1 /etc/vsftpd/vsftpd.conf


![](Pasted%20image%2020260428093408.png)

![](Pasted%20image%2020260428095745.png)

![](Pasted%20image%2020260428100059.png)

- 이렇게 수정
- systemctl enable --now dhcpd
- systemctl status dhcpd


vsftpd 설정 방법 (이외의 궁금한점 virtualtech.tistory 가서 보기)
	vi /etc/vsftpd/vsftpd.conf
	:!bash
	useradd a
	passwd a (It1)

계정 자동
	echo 'It1'
	useradd b
	echo 'It1' | passwd --stdin b

(계정은 암호화되서 /etc/shadow 파일에 잘 저장됨)

![](Pasted%20image%2020260428103436.png)
umask: Linux에서 파일이나 디렉토리를 생성할 때 최고 권한으로 생성 되는 보안상 문제를
해결하기 위해서 최고 권한에서 umask 만큼의 권한을 제거하고 생성함.

파일 최고권한: 666
디렉토리 최고권한: 777 (디렉토리에 접근하기 위해서는 실행 권한 있어야 접근 가능)

touch [파일]: 파일을 현재 시간에 맞춰줌, 만약 파일이 없다면 현재 시간에 맞춰서 생성
ftp의 로고 파일 이름: xferlog
vi 편집기: x누르면 한글자만 지울 수 있음
	r누르면 한글자만 바꿀수있음



28 #ano_upload_enable=YES
39 xferlog_enable=YES
42 connect_from_port_20=YES
52 xferlog_file=/var/log/xferlog
56 xferlog_std_format=YES
62 data_connection_timeout=60
86 ftp_banner=Warning!
101 chroot_list_enable=YES (일반사용자는 최상위 디렉토리를 벗어날 수 없음)
103 chroot_list file=/etc/vsftpd/chroot_list
	vi /etc/vsftpd/chroot_list (a사용자등록)
	
	이걸활성화하면 추가해야하는거 allow_writeable_chroot=YES
128 allow_writeable_chroot=YES

systemctl enable --now vsftpd
systemctl status vsftpd
ss -nat (네트워크 상태를 보자, 서비스명 포트명을 바꾸자, 그중에서 tcp만 보겠다.)
(iptables를 자유자재로 다룰 수 있는가. -- 이게 linux초급중급을 나눔)

![](Pasted%20image%2020260428110039.png)
20번,21번 포트를 열어줘야 함



---

※ 추가사항
![](Pasted%20image%2020260428095314.png)

