---
title: 외부강사님 실습2
date: 2026-04-01
categories:
  - security
comments: true
tags:
  - 방화벽
---




	
	
	실제관리자 ip를 넣음, c클래스넣지않음 --> 시정 권고 들어감
	
	물리적 인터페이스 primary 체크 -> why? 안그러면 _0, _1,... 등 vlan처럼 생김
	
	-nni: 포트번호 다 그대로 보여줌
	-ni: 포트번호는 서비스명으로 변환
	
	내 사설 ip 숨기고 공인ip로 출발: NAT
	
	방화벽 정책은 위->아래 로 적용, 끝까지 없으면 맨 아래의 숨겨진 deny적용(모두차단)
	
	트래픽의 양을 보장받는 것: QoS
	
	HA: 이중화 -> 사용이유: 비싼 L4사용X
	
	L4의 SLB방식
	
	DNS: F/W의 DNS설정, 장비, 무작정 8.8.8.8쓸 필요없음 회사나, 장비에 맞게 사용
	
	방화벽구조: 5tuple구조 -> 제조사마다 다름(6tuple,...)
	서비스 = 목적지포트 + protocol field
	
	ISMS-P 검사 받으면 F/W 부터 검사받음. 특정한 통신을 하는 것에 Any를 썼는가, 어떤 class를
	썼는가, 포트를 왜 열었는가? 이런걸 감사,지적 받음
	
	객체를 만드는게 가장 중요(타이트하게), 일부 회사는 규칙도 존재
	
	빨강:외부/파랑:내부/초록:dmz
	
	원래의 정책 추가 순서
	- 객체 추가, 객체 설정
	- 방화벽 정책 설정
	  
	  방화벽 양방향 설정 굳이x -> 양쪽에서 서로 뭔가 할때만 설정
	  
	  1대1 동시변환 nat
	  
	  출발지는 여러개, 사용가능한 공인ip는 적음 -> port pool 사용
	  공인 ip가 200.1.1.2로 다 나가는데 여기에 포트를 적용
	  200.1.1.2:100, 200.1.1.2:101,... 이런식으로
	  
	  `nat show rsule`로 nat가 어떻게 변환되는지를 볼 수 있음
	  

---

# 브릿지 테스트

	정책으로만 통제 -> 브릿지 모드 사용
	
	브릿지는 ip가 필요없음 -> 관리하기 위해 ip 생성
	
	한명이 mgmt로 바꿈 ip를 10.대 외부망 l2포트 으로
	

# VPN

	지사많은 회사
	
	전용선 + vpn -> 비싸지만 권고중